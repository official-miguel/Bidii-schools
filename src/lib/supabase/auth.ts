/**
 * DEPRECATED — Supabase OTP auth has been removed.
 *
 * Login is now email/phone + password via /api/auth/login.
 * The Supabase client is still used for Storage (file uploads).
 *
 * This file is kept as an empty stub to avoid import errors in any files
 * that haven't been cleaned up yet.  It can be deleted once confirmed safe.
 */

export {};
